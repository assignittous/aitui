section
  md-list.main
    md-list-item("ng-repeat" = "item in attachments.items track by item.id" , "ng-show"="attachments.items.length > 0")
      md-item-content
        .icon
          .content
            a(ng-href="{{item.file_url}}")
              i.icon-cloud.s24
              md-tooltip(md-direction="bottom right") Click to download
        .main
          .content
            .line-1 
              a.name(ng-href="{{item.file_url}}", ng-bind="item.filename")
            .line-2
              | Service Name will go here
        .icon
          .content
            md-button.icon-dark(type="button", ng-click="moreAttachmentActions(item)", aria-label="label", md-no-ink="true")
              i.icon-more.s24
              md-tooltip(md-direction="bottom left") More Actions        
  .blank-slate(ng-show="attachments.items.length == 0")
    h1 There are currently no cloud files.